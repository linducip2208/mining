@extends('layouts.app')
@section('title', ' - Kesehatan Sistem')
@section('content')
<div class="page-frame"><div class="mb-6"><div class="section-kicker">Reliability</div><h1 class="text-[28px] font-bold tracking-[-.03em] text-slate-900">System Health</h1><p class="mt-1 text-sm text-slate-500">Ringkasan kesiapan runtime aplikasi tanpa membuka credential atau secret.</p></div><div class="grid sm:grid-cols-2 xl:grid-cols-3 gap-4 mb-5">@foreach($checks as $label => $ok)<div class="dashboard-card p-4 flex items-center gap-3"><span class="w-10 h-10 rounded-xl flex items-center justify-center {{ $ok ? 'bg-emerald-50 text-emerald-600' : 'bg-red-50 text-red-600' }}"><x-ui.icon :name="$ok ? 'check' : 'alert'" class="w-5 h-5" /></span><div><div class="text-sm font-semibold text-slate-800">{{ \App\Support\HumanLabel::label($label) }}</div><div class="text-xs {{ $ok ? 'text-emerald-600' : 'text-red-600' }}">{{ $ok ? 'Sehat' : 'Perlu perhatian' }}</div></div></div>@endforeach</div><section class="dashboard-card p-5"><h2 class="font-semibold text-slate-900 mb-4">Versi runtime</h2><dl class="grid sm:grid-cols-2 gap-x-8 gap-y-3">@foreach($versions as $label=>$value)<div class="flex justify-between gap-4 border-b border-slate-100 pb-2"><dt class="text-sm text-slate-500">{{ $label }}</dt><dd class="text-sm font-semibold text-slate-800">{{ $value }}</dd></div>@endforeach</dl></section></div>
@endsection
